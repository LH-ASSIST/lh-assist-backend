spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lh_assist_db
    username: postgres
    password: jiny0609
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

app:
  cors:
    allowed-origins:
      - "http://localhost:5173"

logging:
  level:
    com.lh.assist.chatbot: DEBUG